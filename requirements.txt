# Core scientific + deep-RL stack used by the simulator, agents and figures.
# Versions are lower bounds known to work; pin exactly for archival releases.
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
torch>=2.0
tqdm>=4.65
Pillow>=10.0
